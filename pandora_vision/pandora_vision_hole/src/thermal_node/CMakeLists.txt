add_library(${PROJECT_NAME}_thermal
  thermal.cpp
  hole_detector.cpp
  )
target_link_libraries(${PROJECT_NAME}_thermal
  ${PROJECT_NAME}_utils
  ${catkin_LIBRARIES}
  )
add_dependencies(${PROJECT_NAME}_thermal
  ${${PROJECT_NAME}_EXPORTED_TARGETS}
  ${catkin_EXPORTED_TARGETS}
  )

add_library(${PROJECT_NAME}_thermal_cropper
  thermal_cropper.cpp
  )
target_link_libraries(${PROJECT_NAME}_thermal_cropper
  ${PROJECT_NAME}_utils
  ${catkin_LIBRARIES}
  )
add_dependencies(${PROJECT_NAME}_thermal_cropper
  ${catkin_EXPORTED_TARGETS}
  )
